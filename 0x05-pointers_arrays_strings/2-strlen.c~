#include "holberton.h"
#include <stdio.h>
#include <string.h>

/**
*
*int _strlen - returns the length of a string.
*
*@s: string to be checked
*Return: void
*
*/

int _strlen(char *s)
{
	int i;

	i = 0;

	while(s[i] != '\0')
	{
	++i;
	}
	return (i);
}
